# Vela CLI reference

[Documentation home](index.md) · [Getting started](getting-started.md) · [Operations guide](operations.md) · [Configuration](configuration.md)

Generated from Vela's public Typer command tree by `scripts/gen_cli_docs.py`. Do not edit this file by hand. Run the generator after changing a command; the documentation test fails on drift.

## Invocation

Running `vela` with no command opens the TUI. `vela tui` is the explicit equivalent. Append `--help` to any command or group for terminal-native help.

For commands that operate on target-owned state, target selection is deterministic:

1. An explicit `--target`.
2. A non-empty `VELA_TARGET`.
3. The target saved by `vela targets use`.
4. The implicit `local` target.

A deployment YAML's `target:` field is provenance, not command routing.

## Root TUI options

These options belong to the root invocation that opens the TUI. They are not inherited across a subcommand boundary: write `vela model list --target gpu-node`, not `vela --target gpu-node model list`. Each command table below lists the options it actually accepts.

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--version` | no | flag | `false` | Show Vela version and exit. |
| `--configs-dir` | no | path | — | Config directory override. |
| `--target` | no | text | — | Execution target name. |
| `--debug` | no | flag | `false` | Enable structured debug log and Textual devtools. |
| `--debug-log` | no | path | — | Debug JSONL path used with --debug. |
| `--install-completion` | no | flag | — | Install completion for the current shell. |
| `--show-completion` | no | flag | — | Show completion for the current shell, to copy it or customize the installation. |

## Public command map

| Command | Purpose |
| --- | --- |
| `vela tui` | Open the Vela TUI explicitly. |
| `vela doctor` | Check controller and target setup, and print fixes. |
| `vela list` | List valid and invalid configs on the target. |
| `vela run` | Launch a config on the target (or --preview to just print it). |
| `vela smoke` | Launch, wait for READY, verify /v1/models, then stop — agent-side |
| `vela smoke-tui` | Same gate driven through the real TUI headlessly |
| `vela stop` | Stop a detached run by id or config name (--kill for SIGKILL). |
| `vela logs` | Replay a detached run's scrubbed log via the agent (--follow to stream). |
| `vela version` | Print the Vela version. |
| `vela agent connect` | Serve the agent over stdio (used by SSH targets). |
| `vela agent gen-token` | Generate a capability token for authenticating to the agent. |
| `vela agent run` | Run the agent daemon in the foreground on a Unix socket. |
| `vela agent start` | Start the background agent daemon. |
| `vela agent status` | Show local daemon status, or diagnose a target with --target. Target diagnostics use a transient connection; they do not inspect a shared background daemon on the target. |
| `vela agent stop` | Stop the background agent daemon. |
| `vela agent restart` | Restart the background agent daemon. |
| `vela build list` | List target-local vLLM builds. |
| `vela build doctor` | Check the target's build toolchain (uv, compiler, CUDA). |
| `vela build add` | Create a target-local vLLM build with the selected install method. |
| `vela build inspect` | Show one build's metadata and integrity. |
| `vela build adopt` | Register an existing venv as a build. |
| `vela build select` | Mark a build as the active one for launches. |
| `vela build verify` | Re-verify a build's recorded files against disk. |
| `vela build repair` | Repair a broken build in place. |
| `vela build run` | Run a command inside a specific target build. |
| `vela build remove` | Delete a build record (and optionally its venv). |
| `vela config push` | Copy a local config file to the target. |
| `vela config pull` | Copy a config file from the target to local. |
| `vela config lint` | Validate a config file without launching. |
| `vela config edit` | Open a config in $EDITOR. |
| `vela deploy create` | Create a deployment config from CLI options. Requires --model or --model-ref. --smoke cannot be combined with --json. |
| `vela deploy edit` | Set deployment fields non-interactively (--set). |
| `vela deploy clone` | Copy an existing deployment under a new name. |
| `vela deploy from-wrapper` | Import a deployment from a shell wrapper script. |
| `vela deploy delete` | Delete a deployment config. |
| `vela deploy export` | Export a deployment config to a shell wrapper script. |
| `vela model list` | List models known to the target registry. |
| `vela model refresh` | Rescan the Hugging Face cache and refresh the model registry. |
| `vela model inspect` | Show one model entry's metadata and cache state. |
| `vela model adopt` | Register an on-disk model directory as an entry. |
| `vela model pin` | Pin a Hugging Face repo as a referenceable model entry. |
| `vela model verify` | Verify a pinned model's files are fully cached. |
| `vela model download` | Download a pinned model into the target's Hugging Face cache. |
| `vela model remove` | Delete a model entry from the registry. |
| `vela runs list` | List active detached runs on the target. |
| `vela runs prune` | Delete terminal/stale run records; live verified runs are never touched. |
| `vela targets list` | List configured targets (the default is marked with *). |
| `vela targets use` | Set (or --clear) the default target used when --target is omitted. |
| `vela targets add` | Add or update a target in the controller registry. |
| `vela targets bootstrap` | Provision a target over SSH: discover or install its agent, then register it. Omitted connection options reuse a registered SSH target's values. New targets require --host. Unless explicitly provided, the agent command is rediscovered so --install can repair a missing or incompatible agent. |
| `vela targets remove` | Remove a target from the registry. |
| `vela targets test` | Handshake with a target's agent to check it is reachable. |
| `vela targets setup-ssh` | Copy your SSH key to a target with ssh-copy-id. |

## Top-level commands

### `vela tui`

Open the Vela TUI explicitly.

```text
vela tui [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--configs-dir` | no | path | — | Config directory override. |
| `--target` | no | text | — | Execution target name. |
| `--debug` | no | flag | `false` | Enable structured debug log and Textual devtools. |
| `--debug-log` | no | path | — | Debug JSONL path used with --debug. |

### `vela doctor`

Check controller and target setup, and print fixes.

```text
vela doctor [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--target` | no | text | — | Target name to inspect in addition to controller. |
| `--json` | no | flag | `false` | Emit machine-readable setup checks. |

### `vela list`

List valid and invalid configs on the target.

```text
vela list [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--configs-dir` | no | path | — | — |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit machine-readable config list. |

### `vela run`

Launch a config on the target (or --preview to just print it).

```text
vela run <NAME> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `NAME` | yes | text | — |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--configs-dir` | no | path | — | — |
| `--preview` | no | flag | `false` | Print command instead of launching. |
| `--target` | no | text | — | Execution target name. |
| `--build-id` | no | text | — | Target build id or label override. |
| `--model-ref` | no | text | — | Target model entry/display override. |
| `--revision` | no | text | — | Model revision override. |
| `--require-cached` | no | flag | `false` | Fail before launch if a pinned model is not cached on the target. |

### `vela smoke`

Launch, wait for READY, verify /v1/models, then stop — agent-side

```text
vela smoke <NAME> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `NAME` | yes | text | — |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--configs-dir` | no | path | — | — |
| `--target` | no | text | — | Execution target name. |
| `--build-id` | no | text | — | Target build id or label override. |
| `--model-ref` | no | text | — | Target model entry/display override. |
| `--revision` | no | text | — | Model revision override. |
| `--require-cached` | no | flag | `false` | Fail before launch if a pinned model is not cached on the target. |

### `vela smoke-tui`

Same gate driven through the real TUI headlessly

```text
vela smoke-tui <NAME> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `NAME` | yes | text | — |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--configs-dir` | no | path | — | — |
| `--target` | no | text | — | Execution target name. |
| `--build-id` | no | text | — | Target build id or label override. |
| `--model-ref` | no | text | — | Target model entry/display override. |
| `--revision` | no | text | — | Model revision override. |
| `--require-cached` | no | flag | `false` | Fail before launch if a pinned model is not cached on the target. |

### `vela stop`

Stop a detached run by id or config name (--kill for SIGKILL).

```text
vela stop <RUN> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `RUN` | yes | text | Run id or config name to stop. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--target` | no | text | — | Execution target name. |
| `--kill` | no | flag | `false` | Send SIGKILL immediately instead of a graceful stop. |

### `vela logs`

Replay a detached run's scrubbed log via the agent (--follow to stream).

```text
vela logs <RUN_ID> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `RUN_ID` | yes | text | Run id to show logs for. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--follow`, `-f` | no | flag | `false` | Stream new log lines until the run exits. |
| `--lines`, `-n` | no | integer range | `0` | Show only the last N lines (0 = all). |
| `--target` | no | text | — | Execution target name. |

### `vela version`

Print the Vela version.

```text
vela version
```

## `agent` commands

Run or connect to the local Vela agent.

### `vela agent connect`

Serve the agent over stdio (used by SSH targets).

```text
vela agent connect [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--socket` | no | path | — | Connect stdio to an existing agent socket. |

### `vela agent gen-token`

Generate a capability token for authenticating to the agent.

```text
vela agent gen-token [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--bytes` | no | integer | `32` | Random bytes of token entropy; 16 bytes is the minimum. |
| `--install` | no | flag | `false` | Write the token to the default agent token file. |
| `--install-path` | no | path | — | Override the token install path. |
| `--target` | no | text | — | Also install the token on a configured target. |

### `vela agent run`

Run the agent daemon in the foreground on a Unix socket.

```text
vela agent run [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--socket` | no | path | — | Unix socket path for the foreground agent daemon. |
| `--idle-timeout` | no | float | — | Exit after this many idle seconds with no connections or active runs. |

### `vela agent start`

Start the background agent daemon.

```text
vela agent start [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--socket` | no | path | — | Unix socket path for the agent daemon. |
| `--json` | no | flag | `false` | Emit machine-readable daemon start result. |

### `vela agent status`

Show local daemon status, or diagnose a target with --target. Target diagnostics use a transient connection; they do not inspect a shared background daemon on the target.

```text
vela agent status [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--socket` | no | path | — | Unix socket path for the local agent daemon. |
| `--target` | no | text | — | Run a transient diagnostic on a configured target. |
| `--json` | no | flag | `false` | Emit machine-readable status or target diagnostic. |

### `vela agent stop`

Stop the background agent daemon.

```text
vela agent stop [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--socket` | no | path | — | Unix socket path for the agent daemon. |
| `--json` | no | flag | `false` | Emit machine-readable daemon stop result. |

### `vela agent restart`

Restart the background agent daemon.

```text
vela agent restart [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--socket` | no | path | — | Unix socket path for the agent daemon. |
| `--json` | no | flag | `false` | Emit machine-readable daemon restart result. |

## `build` commands

Manage target-local vLLM builds.

### `vela build list`

List target-local vLLM builds.

```text
vela build list [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit machine-readable build list. |

### `vela build doctor`

Check the target's build toolchain (uv, compiler, CUDA).

```text
vela build doctor [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit machine-readable build readiness. |

### `vela build add`

Create a target-local vLLM build with the selected install method.

```text
vela build add [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--method` | yes | text | — | Use build adopt for an existing environment. nightly/commit require uv on the target. pip/wheel/git can fall back to pip. Methods: pip, nightly, commit, git, wheel. |
| `--label` | no | text | — | Build label. |
| `--channel` | no | text | — | Nightly or wheel CUDA channel. |
| `--python` | no | text | — | Requested Python version. |
| `--spec` | no | text | — | Package spec; required for --method pip. |
| `--commit` | no | text | — | vLLM commit SHA; required for --method commit. |
| `--url` | no | text | — | Repository URL; required for --method git. |
| `--ref` | no | text | — | Git ref. |
| `--path` | no | path | — | Wheel path; required for --method wheel. |
| `--precompiled` | no | flag | `false` | Use precompiled vLLM extensions for git builds. |
| `--env` | no | text; repeatable | — | Build environment override KEY=VALUE. |
| `--target` | no | text | — | Execution target name. |

### `vela build inspect`

Show one build's metadata and integrity.

```text
vela build inspect <BUILD> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `BUILD` | yes | text | Build id or label to inspect. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit machine-readable build detail. |

### `vela build adopt`

Register an existing venv as a build.

```text
vela build adopt <VENV_PATH> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `VENV_PATH` | yes | path | External vLLM virtualenv to adopt. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--build-id` | no | text | — | Deprecated; ignored. Build ids are minted. |
| `--label` | no | text | — | Build label. |
| `--vllm-version` | no | text | — | Resolved vLLM version. |
| `--vllm-version-profile` | no | text | — | vLLM version profile label. |
| `--notes` | no | text | — | Operator notes. |
| `--copy` | no | flag | `false` | Copy the external venv into the build directory. |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit machine-readable adoption result. |

### `vela build select`

Mark a build as the active one for launches.

```text
vela build select <BUILD> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `BUILD` | yes | text | Build id or label to make active. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit machine-readable selection result. |

### `vela build verify`

Re-verify a build's recorded files against disk.

```text
vela build verify <BUILD> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `BUILD` | yes | text | Build id or label to verify. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit machine-readable verification result. |

### `vela build repair`

Repair a broken build in place.

```text
vela build repair <BUILD> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `BUILD` | yes | text | Build id or label to repair. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit machine-readable repair result. |

### `vela build run`

Run a command inside a specific target build.

```text
vela build run <BUILD> [--target NAME] -- COMMAND [ARGS...]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `BUILD` | yes | text | Build id or label to run. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--target` | no | text | — | Execution target name. |

Everything after `--` is executed by the selected build on the target.
The controller never resolves the target's virtual-environment path.

### `vela build remove`

Delete a build record (and optionally its venv).

```text
vela build remove <BUILD> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `BUILD` | yes | text | Build id or label to remove. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--configs-dir` | no | path | — | Config directory for pin checks. |
| `--target` | no | text | — | Execution target name. |
| `--yes` | no | flag | `false` | Confirm removing the agent-owned build directory. |
| `--json` | no | flag | `false` | Emit machine-readable removal result. |

## `config` commands

Move and lint target-local deployment configs.

### `vela config push`

Copy a local config file to the target.

```text
vela config push <FILE> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `FILE` | yes | path | Local config YAML to push to the target. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--configs-dir` | no | path | — | Target config directory override. |
| `--target` | no | text | — | Execution target name. |
| `--overwrite` | no | flag | `false` | Overwrite an existing target config. |
| `--json` | no | flag | `false` | Emit machine-readable push result. |

### `vela config pull`

Copy a config file from the target to local.

```text
vela config pull <NAME> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `NAME` | yes | text | Target config name to pull. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--configs-dir` | no | path | — | Target config directory override. |
| `--target` | no | text | — | Execution target name. |
| `--output`, `-o` | no | path | — | Local path to write the pulled YAML. |
| `--json` | no | flag | `false` | Emit machine-readable pull result. |

### `vela config lint`

Validate a config file without launching.

```text
vela config lint <FILE> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `FILE` | yes | path | Local config YAML to lint. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit machine-readable lint result. |

### `vela config edit`

Open a config in $EDITOR.

```text
vela config edit <NAME> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `NAME` | yes | text | Target config name to edit. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--configs-dir` | no | path | — | Target config directory override. |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit machine-readable edit result. |

## `deploy` commands

Create and manage target-local deployments.

### `vela deploy create`

Create a deployment config from CLI options. Requires --model or --model-ref. --smoke cannot be combined with --json.

```text
vela deploy create <NAME> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `NAME` | yes | text | Deployment/config name to create. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--model` | no | text | — | Launch model repo id, URL, or target-local path. |
| `--model-ref` | no | text | — | Target model registry entry/display name. |
| `--revision` | no | text | — | Model revision override. |
| `--runtime` | no | text | `process` | Runtime: process, docker, build, or executable. |
| `--image` | no | text | — | Pinned docker image/tag/digest for docker runtime. |
| `--build` | no | text | — | Target build id or label for process runtime. |
| `--executable` | no | path | — | Target-local vLLM executable path. |
| `--preset` | no | text | — | Deployment composer preset. |
| `--port` | no | text | — | Preferred port, or 'auto' to allocate. |
| `--host` | no | text | — | Server bind host override. |
| `--exposure` | no | text | — | Exposure override: local, lan, or public. |
| `--set` | no | text; repeatable | — | Override a config field, e.g. engine.kv_cache_dtype=fp8. |
| `--extra-arg` | no | text; repeatable | — | Append one raw vLLM serve arg. Use --extra-arg=--flag for flag values. |
| `--configs-dir` | no | path | — | Target config directory override. |
| `--target` | no | text | — | Execution target name. |
| `--dry-run` | no | flag | `false` | Compose, validate, and preview without saving. |
| `--overwrite` | no | flag | `false` | Overwrite an existing config of the same name. |
| `--force` | no | flag | `false` | Save even when preflight fails (warnings still print). |
| `--smoke` | no | flag | `false` | Run a bounded smoke after saving. |
| `--json` | no | flag | `false` | Emit machine-readable compose result. |

### `vela deploy edit`

Set deployment fields non-interactively (--set).

```text
vela deploy edit <NAME> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `NAME` | yes | text | Deployment/config name to edit. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--port` | no | text | — | Preferred port, or 'auto' to leave unchanged. |
| `--host` | no | text | — | Server bind host override. |
| `--exposure` | no | text | — | Exposure override: local, lan, or public. |
| `--set` | no | text; repeatable | — | Override a config field, e.g. engine.kv_cache_dtype=fp8. |
| `--extra-arg` | no | text; repeatable | — | Append one raw vLLM serve arg. Use --extra-arg=--flag for flag values. |
| `--configs-dir` | no | path | — | Target config directory override. |
| `--target` | no | text | — | Execution target name. |
| `--dry-run` | no | flag | `false` | Validate edits without saving. |
| `--json` | no | flag | `false` | Emit machine-readable edit result. |

### `vela deploy clone`

Copy an existing deployment under a new name.

```text
vela deploy clone <SRC_NAME> <NEW_NAME> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `SRC_NAME` | yes | text | Existing deployment/config name. |
| `NEW_NAME` | yes | text | New deployment/config name. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--set` | no | text; repeatable | — | Override a config field, e.g. server.port=18005. |
| `--extra-arg` | no | text; repeatable | — | Append one raw vLLM serve arg. Use --extra-arg=--flag for flag values. |
| `--configs-dir` | no | path | — | Target config directory override. |
| `--target` | no | text | — | Execution target name. |
| `--overwrite` | no | flag | `false` | Overwrite an existing cloned config. |
| `--json` | no | flag | `false` | Emit machine-readable clone result. |

### `vela deploy from-wrapper`

Import a deployment from a shell wrapper script.

```text
vela deploy from-wrapper <SRC_NAME> [NEW_NAME] [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `SRC_NAME` | yes | text | Existing wrapper-based deployment/config name. |
| `NEW_NAME` | no | text | New native-docker deployment/config name. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--configs-dir` | no | path | — | Target config directory override. |
| `--target` | no | text | — | Execution target name. |
| `--dry-run` | no | flag | `false` | Emit the migrated config without saving. |
| `--overwrite` | no | flag | `false` | Overwrite an existing migrated config. |
| `--json` | no | flag | `false` | Emit machine-readable migration result. |

### `vela deploy delete`

Delete a deployment config.

```text
vela deploy delete <NAME> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `NAME` | yes | text | Deployment/config name to delete. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--configs-dir` | no | path | — | Target config directory override. |
| `--target` | no | text | — | Execution target name. |
| `--yes` | no | flag | `false` | Confirm deleting the target-local config. |
| `--json` | no | flag | `false` | Emit machine-readable delete result. |

### `vela deploy export`

Export a deployment config to a shell wrapper script.

```text
vela deploy export <NAME> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `NAME` | yes | text | Deployment/config name to export. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--output` | no | path | — | Target-local path to write the standalone docker script. |
| `--configs-dir` | no | path | — | Target config directory override. |
| `--target` | no | text | — | Execution target name. |
| `--overwrite` | no | flag | `false` | Overwrite an existing exported script. |
| `--json` | no | flag | `false` | Emit machine-readable export result. |

## `model` commands

Manage target-local model metadata.

### `vela model list`

List models known to the target registry.

```text
vela model list [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--target` | no | text | — | Execution target name. |
| `--cached-only` | no | flag | `false` | Show only cached models. |
| `--pinned-only` | no | flag | `false` | Show only registry-pinned models. |
| `--json` | no | flag | `false` | Emit machine-readable model list. |

### `vela model refresh`

Rescan the Hugging Face cache and refresh the model registry.

```text
vela model refresh [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit machine-readable refresh result. |

### `vela model inspect`

Show one model entry's metadata and cache state.

```text
vela model inspect <MODEL_REF> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `MODEL_REF` | yes | text | Model entry id or display name. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit machine-readable model detail. |

### `vela model adopt`

Register an on-disk model directory as an entry.

```text
vela model adopt <LOCAL_PATH> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `LOCAL_PATH` | yes | path | Local model directory to adopt. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--entry-id` | no | text | — | Deprecated; ignored. Entry ids are minted. |
| `--display-name`, `--name` | no | text | — | Human-readable model name. |
| `--quant-format` | no | text | — | Model quantization label. |
| `--tokenizer` | no | text | — | Tokenizer override. |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit machine-readable adoption result. |

### `vela model pin`

Pin a Hugging Face repo as a referenceable model entry.

```text
vela model pin <REPO_OR_ENTRY> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `REPO_OR_ENTRY` | yes | text | Hugging Face repo id, URL display name, or legacy entry id when --repo-id is supplied. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--entry-id` | no | text | — | Deprecated; ignored. Entry ids are minted. |
| `--repo-id` | no | text | — | Hugging Face repo id. |
| `--display-name`, `--name` | no | text | — | Human-readable model name. |
| `--revision` | no | text | — | Revision, branch, tag, or commit sha. |
| `--commit-sha` | no | text | — | Resolved immutable commit sha. |
| `--local-path` | no | path | — | Adopt a local model directory. |
| `--url` | no | text | — | Remote model URL. |
| `--quant-format` | no | text | — | Model quantization label. |
| `--tokenizer` | no | text | — | Tokenizer override. |
| `--gated` | no | flag | `false` | Mark model metadata as gated. |
| `--token-required` | no | flag | `false` | Mark model metadata as requiring HF_TOKEN. |
| `--notes` | no | text | — | Operator notes. |
| `--new` | no | flag | `false` | Mint a new entry instead of upserting an existing repo-id pin. |
| `--offline` | no | flag | `false` | Skip the Hugging Face metadata lookup; trust the supplied fields and record the pin as unvalidated (validated: false). |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit machine-readable pin result. |

### `vela model verify`

Verify a pinned model's files are fully cached.

```text
vela model verify <MODEL_REF> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `MODEL_REF` | yes | text | Model entry id or display name. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--target` | no | text | — | Execution target name. |
| `--deep` | no | flag | `false` | Run deep content-hash verification. |
| `--json` | no | flag | `false` | Emit machine-readable verification result. |

### `vela model download`

Download a pinned model into the target's Hugging Face cache.

```text
vela model download <MODEL_REF> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `MODEL_REF` | yes | text | Model entry id or display name. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--revision` | no | text | — | Revision override for the download job. |
| `--allow` | no | text; repeatable | — | Allow-pattern passed to Hugging Face download. |
| `--ignore` | no | text; repeatable | — | Ignore-pattern passed to Hugging Face download. |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit final job result as machine-readable JSON. |

### `vela model remove`

Delete a model entry from the registry.

```text
vela model remove <MODEL_REF> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `MODEL_REF` | yes | text | Model entry id or display name. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--configs-dir` | no | path | — | Config directory for pin checks. |
| `--target` | no | text | — | Execution target name. |
| `--yes` | no | flag | `false` | Confirm removing model metadata. |
| `--force` | no | flag | `false` | Override config pin protection. |
| `--json` | no | flag | `false` | Emit machine-readable removal result. |

## `runs` commands

Inspect target-side active runs and prune controller-local run records.

### `vela runs list`

List active detached runs on the target.

```text
vela runs list [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--target` | no | text | — | Execution target name. |
| `--json` | no | flag | `false` | Emit machine-readable run list. |

### `vela runs prune`

Delete terminal/stale run records; live verified runs are never touched.

```text
vela runs prune [OPTIONS]
```

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--runs-dir` | no | path; repeatable | — | Runs directory to prune (repeatable). Defaults to this host's runs dir. |
| `--keep` | no | integer range | `20` | Keep this many newest eligible run records per directory. |
| `--older-than-days` | no | float range | `7.0` | Only prune run records whose newest file is older than this many days. |
| `--dry-run` | no | flag | `false` | Report what would be pruned without deleting. |

## `targets` commands

Manage controller target registry.

### `vela targets list`

List configured targets (the default is marked with *).

```text
vela targets list
```

### `vela targets use`

Set (or --clear) the default target used when --target is omitted.

```text
vela targets use [NAME] [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `NAME` | no | text | Target name to make the default (omit with --clear). |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--clear` | no | flag | `false` | Clear the saved default target. |

### `vela targets add`

Add or update a target in the controller registry.

```text
vela targets add <NAME> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `NAME` | yes | text | Target name to add or update. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--transport` | no | local \| ssh | `ssh` | Target transport kind. |
| `--host` | no | text | — | SSH host. |
| `--ssh-key` | no | path | — | SSH private key path for this target. |
| `--workdir` | no | path | — | Remote working directory. |
| `--venv` | no | path | — | Remote virtualenv path. |
| `--agent-command` | no | text | — | Remote agent command, shell-split safely. |
| `--ssh-opts-env` | no | text | — | Environment variable with SSH options. |

### `vela targets bootstrap`

Provision a target over SSH: discover or install its agent, then register it. Omitted connection options reuse a registered SSH target's values. New targets require --host. Unless explicitly provided, the agent command is rediscovered so --install can repair a missing or incompatible agent.

```text
vela targets bootstrap <NAME> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `NAME` | yes | text | Target name to add or update. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--host` | no | text | — | SSH host, e.g. user@host. Required for new targets; registered SSH targets reuse their existing host when omitted. |
| `--ssh-key` | no | path | — | SSH private key path for this target. |
| `--workdir` | no | path | — | Remote working directory. |
| `--venv` | no | path | — | Remote virtualenv path. |
| `--agent-command` | no | text | — | Remote agent command, shell-split safely. |
| `--ssh-opts-env` | no | text | — | Environment variable with SSH options. |
| `--install` | no | flag | `false` | Install the Vela agent into the managed target venv. |
| `--install-spec` | no | text | `vela @ git+https://github.com/bgconley/vela.git` | Python package spec used by --install. |
| `--build` | no | text | — | Create a default pip build from this package spec. |

### `vela targets remove`

Remove a target from the registry.

```text
vela targets remove <NAME>
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `NAME` | yes | text | Target name to remove. |

### `vela targets test`

Handshake with a target's agent to check it is reachable.

```text
vela targets test [NAME]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `NAME` | no | text | Target name to test. |

### `vela targets setup-ssh`

Copy your SSH key to a target with ssh-copy-id.

```text
vela targets setup-ssh <NAME> [OPTIONS]
```

**Arguments**

| Argument | Required | Type | Description |
| --- | --- | --- | --- |
| `NAME` | yes | text | Target name to configure. |

**Options**

| Option | Required | Value | Default | Description |
| --- | --- | --- | --- | --- |
| `--identity`, `-i` | no | path | — | SSH public/private key path for ssh-copy-id. |

## Hidden compatibility aliases

These remain accepted for compatibility but are intentionally absent from normal help. Use the canonical spelling in scripts and documentation.

| Compatibility alias | Canonical command |
| --- | --- |
| `vela preview NAME ...` | `vela run NAME --preview ...` |
| `vela deploy list ...` | `vela list ...` |
| `vela model add ...` | `vela model pin ...` |

## Automation and exit-status rules

`--json` changes output shape. Commands that validate or mutate state now preserve their failure exit where stated below, but consumers should still inspect the returned field (`ok`, `preflight_ok`, job status, or equivalent) as well as the process exit code.

- Typer usage errors, ambiguous references, transport failures, and missing destructive `--yes` confirmation normally exit `2`.
- Attached `vela run` and `vela logs --follow` return the eventual run code; a successful detached launch returns `0`.
- `vela smoke` returns `0` only after READY and `/v1/models` verification, uses `2` for classified failures, and can use `1` for generic not-ready paths.
- `vela smoke-tui` returns `0` for success and `2` for failure.
- Build/model jobs normally return `0` for success and `2` for a failed job.
- `vela config lint --json` emits its payload and returns `1` when `ok` is false. Build verify/repair and model verify emit their result and return `2` on failure.
- `vela deploy create --json` never bypasses preflight: a failed preflight returns `2`, reports `preflight_ok: false`, and does not save unless `--force` was explicitly supplied.
- `vela runs prune` operates on controller-local artifact directories and has no target option; begin with `--dry-run`.

## Related references

- [Configuration schema and discovery](configuration.md)
- [Environment and storage paths](environment.md)
- [TUI key reference](tui.md)
- [Troubleshooting](troubleshooting.md)
