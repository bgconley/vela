name: fake-child
description: No-GPU fake child for smoke testing Vela.
model: fake/model
served_model_name: fake-model
command:
  entrypoint: serve
  executable: ./scripts/fake_vllm_child.py
server:
  host: 127.0.0.1
  port: 8765
logging:
  request_logging: false
launch:
  mode: attached
  ready_timeout_seconds: 30
